import React from 'react';

function Story({ username, img }) {
	return (
		<div className="">
			<img className="" src={img} alt="" />
			{/* <p>{username}</p> */}
		</div>
	);
}

export default Story;
